<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.a.aa.mapper.MemberMapper">

        <insert id="join">
            insert into p_user values(
                p_user_seq.nextval,
                #{id},
                #{password},
                #{name},
                sysdate,
                #{role}
            )
        </insert>

        <select id="memberList" resultType="com.a.aa.domain.MemberListDTO">
            select mno, id, name, created_date, role
            from p_user_view
            order by mno
        </select>

        <select id="login" resultType="com.a.aa.domain.LoginDTO">
            select mno, id, name, type from p_user_view
            where id = #{id} and password = #{password}
        </select>
</mapper>